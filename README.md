The HRM repository, adapted for the task of learning chess. To start do the following:
1. Install dependencies by running `pip install -r requirements`
2. Generate the Chess dataset by running `dataset/build_chess_dataset.py`
3. Run training by running `run_chess_training.py`
(Optional) Visualise the generated data by running the file `run_dataset_visualization.py`